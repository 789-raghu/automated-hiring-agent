import express, { Response, Request } from "express";

const router = express.Router();

router.get("/", (req: Request, res: Response) => {});

router.post("/create-assessment", (req: Request, res: Response) => {});

export default router;
